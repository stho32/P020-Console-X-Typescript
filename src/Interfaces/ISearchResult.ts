/**
 * That is the thing that we want back
 */
interface ISearchResult {
    link: string;
    name: string;
    description: string;
    availability: boolean;
    rating: number;
    price: number;
}
